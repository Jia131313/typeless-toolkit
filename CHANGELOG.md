# 更新日志

## 1.7.0 - 跨设备账号词库同步与工具集更新 (2026-09-07)

本版适合在多台 Windows / Mac 之间使用 Typeless 的用户：可通过 WebDAV 合并保存的账号和词库，并在工具集内检查后续版本。原有本地账号、快照、词库和配置继续保留。

### 跨设备同步

- 新增「WebDAV 同步」，支持坚果云及通用 WebDAV，可选仅账号、仅词库或两者；配置会记忆，密码留空沿用已保存值。各设备需使用相同云端路径和同步密码。
- 不同设备的账号按 ID 合并。例如 A 有 1、2、3，B 有 7、8、9，完成同步后两边都有六个账号；空白设备不会清空云端。
- 明确删除会同步到其他设备，离线旧数据不会复活；重新收录账号或重新添加词条可以恢复。删除管理记录不会注销 Typeless 账号。
- 修复上传期间本地增删被旧结果覆盖、自动续签误更新同步时间的问题；并发写入会重读合并，无变化时不重复上传。
- 启动、账号变更和词库编辑后自动同步，运行期间每 15 分钟检查一次；拉取的词库继续自动对齐到已管理账号。
- 账号与词库分别加密；不上传短期 access token、设备 ID 或登录快照。新设备收到云端账号后，点击「在此设备启用」建立本机登录态。

### 工具集更新

- 新增独立的「工具集更新」入口，可检查 GitHub Release、查看版本说明、下载并校验对应更新包。
- Windows 保持当前 Portable / Lite 类型，确认更新后退出、替换程序并重启；保留 `data/`。仅暂存本次旧程序，成功或恢复后清理。
- macOS 下载并校验 Universal DMG，打开后仍需拖入「应用程序」替换；账号等数据保存在 App 外。
- 「安装 Typeless 官方更新」继续只处理 Typeless 本体，与 Toolkit 更新分别显示。

从 v1.6.x 升级到本版仍需按下方说明手动安装一次；「工具集更新」用于本版之后的更新。Windows 发布包包含 Portable 和 Lite，macOS 包为 Apple Silicon / Intel 通用版。

## 1.6.2 - 引导旧账号更新长期凭证 (2026-09-01)

- 修复 v1.6.1 升级后旧账号只有短期 access token 时有效期显示为“—”或“0 天”却没有明确引导的问题。
- 旧账号现在明确显示“需更新”；点击右上角当前账号即可打开收录窗口，重新抓取并原位保存长期 refresh token。

## 1.6.1 - 长期登录凭证与账号收录优化 (2026-09-01)

- 适配 Typeless 2.3.1+ 的短期 access token 与长期 refresh token：收录账号时同时保存两种凭证，并在 API 调用前按需续签。
- 登录有效期改为按 refresh token 展示；旧版仅有 access token 的账号继续兼容，过期后需重新收录一次。
- “添加当前账号”移到“注册账号”右侧；点击右上角未收录账号时直接打开收录窗口。
- 公开包首次启动使用空账号列表，不再附带无实际用途的示例账号。

## 1.6.0 - 权限修复与默认自动化 (2026-08-22)

### macOS 权限与启动

- 所有 Typeless 启动统一改由 LaunchServices 打开 App Bundle，修复直接启动二进制造成的麦克风、辅助功能权限归因错误和切号后循环授权。
- Typeless 补丁或官方更新改变代码身份时，仅在确有变化时清理失效的麦克风与辅助功能记录，避免系统显示“已开启”但新进程无法识别。
- 工具集自身更新后识别 ad-hoc 签名身份变化，清理旧 App 管理及历史误加的辅助功能记录，并提供可恢复的授权流程。

### 默认自动化

- 新增账号、修改词库和工具集启动后默认自动对齐所有账号词库，增加 single-flight、变更去抖、15 分钟兜底检查和明确删除墓碑。
- 工具集启动、账号变更及 Typeless 官方更新后自动检查并维护弹窗补丁；Typeless 正在使用时延后侵入式修补，文件未变化时跳过完整扫描。
- macOS 缺少 App 管理权限时直接打开对应设置，用户授权返回后自动继续原操作；原有手动按钮保留为状态查看和失败重试入口。

### 界面与可靠性

- 保留全部原有工具栏入口，精简词库、账号、备份、官方更新和权限相关标签；默认宽度保持单行，窄窗口按语义组换行。
- 增加自动词库同步、自动补丁维护、LaunchServices 启动和代码身份迁移回归测试。

## 1.5.0 - macOS 安全补丁与跨平台发布 (2026-08-10)

### macOS

- 完成账号管理、账号切换、词库同步、设备重置、注册流程、跳过教程和去弹窗功能对齐。
- 去弹窗前创建完整 App 回滚快照，定向重签 Electron Framework 与根 Bundle，并保留运行权限和 Hardened Runtime。
- 增加补丁后签名、启动存活检查、失败诊断日志及事务回滚。
- 修复辅助功能身份变化提示、Spotlight 重复应用、应用内图标、单实例与端口冲突问题。
- 深色模式同步 macOS 原生标题栏；重写“注册并添加新账号”流程说明。
- 增加 `npm run deploy:mac`，自动构建、替换、验证并复用现有用户数据。

### 通用

- 当前账号日常检测改为只读本地存储，不再隐式重启 Typeless 或开启 CDP。
- 新手引导状态覆盖 live 文件与账号快照，切号后可自动补齐缺失标记。
- 本地 HTTP 服务验证 Host/Origin，账号列表不再向前端返回 bearer token。
- 增加桌面宿主、管理器安全、新手引导、macOS 签名及官方升级测试。
- 新增标签触发的 GitHub Actions，统一构建 Windows Lite/Portable 与 macOS Universal DMG，并发布 SHA-256 校验文件。
