{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The local manager page receives no generic filesystem or shell capability.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
